node_modules/
dist/
*.log

# editor and OS junk
.DS_Store
._*
Thumbs.db
Desktop.ini

# keep secrets out of the repo: your API key lives in an env var or an
# untracked file, never in a tracked one
.env
.env.*
.envrc
mise.local.toml
*.pem

# Claude Code's project directory is machine-local: settings in here can hold
# your course API key, and this repo goes public when you ship. Your tracked
# harness is CLAUDE.md (and any AGENTS.md), not this directory.
.claude/

# Full third-party asset pack dropped into public/ for sourcing sprites from
# --- only the handful actually used get copied into public/sprites/ (tracked
# below) and shipped; the pack itself is 187 files of spritesheets, previews
# and licensing text nobody needs in the deployed site.
public/Platformer Assets Extra Animations & Enemies/
